export const metadata = {
  title: "Dashboard",
};

export const dynamic = "force-dynamic";

const Dashboard = () => {
  return (
    <>
      <div>Dashboard</div>
    </>
  );
};

export default Dashboard;
